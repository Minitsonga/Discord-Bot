<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">

    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/guild_Dashboard.css">
    <link rel="stylesheet" href="/css/sidebars.css">
    <link href='https://unpkg.com/boxicons@2.0.9/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flag-icon-css/3.5.0/css/flag-icon.min.css"
        integrity="sha512-Cv93isQdFwaKBV+Z4X8kaVBYWHST58Xb/jVOcV9aRsGSArZsgAnFIhMpDoMDcFNoUtday1hdjn0nGp3+KZyyFw=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <script src="https://kit.fontawesome.com/dead68bbd8.js" crossorigin="anonymous"></script>

    <link rel="icon" href="img/Bot.jpg" type="image/icon type">
    <title>Secrétaire - <%=guild.name%></title>
</head>

<body>
    <%- include("../templates/svgIcons") -%>

        <!--MAIN BODY-->
        <%- include("../templates/sidebarPlugins") -%>
            <section class="home-section">
                <h1 class="ps-3 pt-4 pb-4">Dashboard</h1>
                <section class="stats">
                    <div class="container py-4">
                        <header class="pb-3 mb-4 border-bottom">
                            <div class="d-flex align-items-center text-white text-decoration-none">
                                <svg class="bi me-2" width="3em" height="3em">
                                    <use xlink:href="#stats" />
                                </svg>
                                <span class="fs-1">Statistics</span>
                            </div>
                        </header>

                        <div class="row align-items-md-center">
                            <div class="col-md-6">
                                <div class="h-100 p-4 text-white bg-purple-normal rounded-3">
                                    <div class="d-flex justify-content-center">
                                        <svg class="bi me-3" width="3em" height="3em">
                                            <use xlink:href="#person-fill" />
                                        </svg>
                                        <h2 class="my-auto">Online Members</h2>
                                    </div>
                                    <div class="d-flex justify-content-center">
                                        <h2 class="pt-3 mb-0">1820</h2>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="h-100 p-4 bg-purple-lighter rounded-3">
                                    <div class="d-flex justify-content-center">
                                        <svg class="bi me-3" width="3em" height="3em">
                                            <use xlink:href="#people-fill" />
                                        </svg>
                                        <h2 class="my-auto">Total Members</h2>
                                    </div>
                                    <div class="d-flex justify-content-center">
                                        <h2 class="pt-3 mb-0">8290</h2>
                                    </div>
                                </div>
                            </div>
                        </div>
                </section>

                <section class="leaderboard">
                    <div class="container py-4">
                        <header class="pb-3 mb-4 border-bottom">
                            <div class="d-flex align-items-center text-white text-decoration-none">
                                <svg class="bi me-2" width="3em" height="3em">
                                    <use xlink:href="#medal" />
                                </svg>
                                <span class="fs-1">Leaderboard</span>
                            </div>
                        </header>

                        <div class="p-4 mb-4 bg-purple rounded-3">
                            <div class="container-fluid">
                                <div class="d-flex flex-column">
                                    <% if(leaderList===null) { %>
                                        <h1>Nobody has spoke in your guild. Earn some experience by talking in your
                                            server to be
                                            able to have a leaderboard</h1>
                                        <% } else {%>
                                            <% leaderList.forEach((userboard,count)=> { %>
                                                <% if(count <=2){ %>
                                                    <%- include('../templates/leaderboardGrid', {userboard}) -%>
                                                        <% }%>
                                                            <%})%>
                                                                <%}%>

                                                                    <a href="#"
                                                                        class="btn bg-outline-other text-white m-auto mt-4 ">
                                                                        <span>View more</span>
                                                                    </a>
                                </div>

                            </div>
                        </div>
                    </div>
                </section>

                <section class="settings">
                    <div class="container py-4">
                        <header class="pb-3 mb-4 border-bottom">
                            <div class="d-flex align-items-center text-white text-decoration-none">
                                <svg class="bi me-2" width="2.5em" height="2.5em">
                                    <use xlink:href="#gear" />
                                </svg>
                                <span class="fs-1">Settings</span>
                            </div>
                        </header>

                        <div class="p-4 mb-4 bg-purple rounded-3">
                            <div class="container-fluid">
                                <div class="d-flex justify-content-start">
                                    <svg class="bi me-3" width="2em" height="2em">
                                        <use xlink:href="#translate" />
                                    </svg>
                                    <h2 class="my-auto">Langages</h2>
                                </div>
                                <h4 class="mt-4">Default langage is English. French will come later.</h4>

                            </div>
                        </div>

                    </div>

                </section>
            </section>
            <div class="container p-4 bg-purple-normal rounded-3 apply-btn-holder" id="holderApply">
                <div class="apply-btn-inside text-white" style="flex: 1 1 0%;">
                    <strong style="font-size: 25px;">Attention !</strong>
                    <p>Appliquez les changements pour que la
                        mise a jour ce fasse sur votre serveur<br>Vous pouvez faire une mise a jour que toutes les
                        <b>30 minutes</b>
                        <br><br>
                        Si vous n'avez pas fini de paramètrer le bot, appuyez en dehors de cette fenêtre ou sur le
                        boutton "Cancel".
                    </p>

                    <a href="#" class="btn bg-purple-animation text-white" id="cancelBtn">Cancel</a>
                    <a type="submit" class="btn bg-outline-lighter" id="apply">Apply changes</a>
                </div>
            </div>

            <div id="overlay"></div>


            <script>
                const applyPopUp = document.querySelector('.apply-btn-holder')
                const closeApplyPopUp = document.querySelector('#cancelBtn')
                const overlay = document.getElementById('overlay')

                let reasonUpdate = "undefined"


                overlay.addEventListener('click', () => {

                    closePopUp(applyPopUp)

                })
                let count = 0;
                if ('<%= pluginsData.welcome %>' !== '<%= prevPlugins.welcome %>') {
                    count += 1;
                }
                if ('<%= pluginsData.birthday %>' !== '<%= prevPlugins.birthday %>') {
                    count += 1;
                }
                if ('<%= pluginsData.moderator %>' !== '<%= prevPlugins.moderator %>') {
                    count += 1;
                }
                if ('<%= pluginsData.suggestion %>' !== '<%= prevPlugins.suggestion %>') {
                    count += 1;
                }
                if ('<%= pluginsData.channelManager %>' !== '<%= prevPlugins.channelManager %>') {
                    count += 1;
                }
                if ('<%= pluginsData.leveling %>' !== '<%= prevPlugins.leveling %>') {
                    count += 1;
                }
                if ('<%= pluginsData.supportTicket %>' !== '<%= prevPlugins.supportTicket %>') {
                    count += 1;
                }
                if ('<%= pluginsData.stream %>' !== '<%= prevPlugins.stream %>') {
                    count += 1;
                }

                if ('<%= pluginsData.economy %>' !== '<%= prevPlugins.economy %>') {
                    count += 1;
                }

                console.log(count, '<%= serverStats.needUpdate %>');
                if (count >= 1 && '<%= serverStats.needUpdate %>' == 'false') {

                    openPopUp(applyPopUp)
                    count = 0;
                    reasonUpdate = "plugin";
                }
               
                else {
                    applyPopUp.style.display = "none"
                    applyPopUp.classList.remove('active');
                }


                closeApplyPopUp.addEventListener('click', () => {
                    closePopUp(applyPopUp)
                })

                function openPopUp(popUp) {
                    if (popUp == null) return
                    popUp.classList.add('active')
                    overlay.classList.add('active')
                }

                function closePopUp(popUp) {
                    if (popUp == null) return
                    popUp.classList.remove('active')
                    overlay.classList.remove('active')
                }
            </script>

            <script>
                const applyBtn = document.querySelector("#apply");
                const applyPopUpHolder = document.querySelector('.apply-btn-holder')

                let data = {reasonUpdate}
                applyBtn.addEventListener("click", () => {
                    closePopUp(applyPopUpHolder);
                    fetch("<%= guild.id%>", {
                        headers: {
                            'Accept': 'application/json',
                            'Content-Type': 'application/json'
                        },
                        method: "POST",
                        body : JSON.stringify(data)
                    }).then((res) => {
                        console.log("done");
                        res.redirect(this)


                    });
                })

            </script>




            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js "
                integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ "
                crossorigin="anonymous "></script>
            <script src="/js/sidebars.js "></script>


            <script>
                const checkIconPlugins = document.querySelectorAll(".bxs-check-circle");

                console.log("here");
                console.log(checkIconPlugins);
                checkIconPlugins.forEach(pluginsIcon => {

                    switch (pluginsIcon.id) {
                        case 'welcome-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.welcome %>' === 'true') ? '#089402' : '#646464';

                            break;

                        case 'birthday-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.birthday %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'moderator-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.moderator %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'suggest-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.suggestion %>' === 'true') ? '#089402' : '#646464';
                            break; suggest

                        case 'channel-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.channelManager %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'leveling-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.leveling %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'ticket-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.supportTicket %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'stream-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.stream %>' === 'true') ? '#089402' : '#646464';
                            break;

                        case 'economy-plugin':
                            document.getElementById(pluginsIcon.id).style.color = ('<%= pluginsData.economy %>' === 'true') ? '#089402' : '#646464';
                            break;
                    }
                })

            </script>


</body>

</html>